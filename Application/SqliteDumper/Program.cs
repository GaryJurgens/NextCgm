using Microsoft.Data.Sqlite; using var connection = new SqliteConnection("Data Source=C:\\Docker\\nginx-ui\\nginx-ui\\database.db"); connection.Open(); var command = connection.CreateCommand(); command.CommandText = "SELECT * FROM sites;"; using var reader = command.ExecuteReader(); while (reader.Read()) { for(int i=0; i<reader.FieldCount; i++) Console.Write(reader.GetName(i) + "=" + reader.GetValue(i) + ", "); Console.WriteLine(); }
